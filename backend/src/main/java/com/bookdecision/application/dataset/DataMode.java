package com.bookdecision.application.dataset;

/** Selects how a request combines the published base dataset with one private upload. */
public enum DataMode {
    SYSTEM_ONLY,
    USER_ONLY,
    USER_OVERLAY
}
