# H5 开发模式通过 Vite 代理访问本机 8080 端口，避免浏览器跨域。
VITE_API_BASE_URL=/backend

# 完整 Compose 不发布后端 8080；微信开发者工具本机联调时使用 H5/Nginx 的回环入口，
# 并仅在开发设置中关闭合法域名校验。备案和 HTTPS 完成前不填写虚假生产域名。
# VITE_API_BASE_URL=http://127.0.0.1:18081/backend
